/*
package Xeocas.Commands;

import org.bukkit.command.Command;
import org.bukkit.command.CommandExecutor;
import org.bukkit.command.CommandSender;

//I don't remember why this was created oh yeah to get the factory block from the secondary menu which is got from the factory main which is got from the ak47 or kar98... bbruh
public class WeaponsFactoryCommand implements CommandExecutor {

    @Override
    public boolean onCommand(CommandSender commandSender, Command command, String s, String[] strings) {


        return true;
    }
}
*/